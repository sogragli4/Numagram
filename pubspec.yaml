name: nonogram_daily
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
# State management is pinned to exact versions (flutter_riverpod /
# riverpod_annotation / riverpod_generator, and isar_community /
# isar_community_flutter_libs / isar_community_generator below): it is the
# only mutually-compatible combination with isar_community_generator's
# analyzer window on this machine's Dart SDK (3.9.2). Bump the whole group
# together once the toolchain moves to Dart >=3.12.
#
# isar_community is an actively maintained fork of Isar with modern
# analyzer/source_gen support; API-compatible with upstream `isar`, which is
# pinned to analyzer <6.0.0 and conflicts with riverpod_generator.
dependencies:
  app_tracking_transparency: ^2.0.7
  applovin_max: ^4.6.4
  cupertino_icons: ^1.0.8
  firebase_analytics: ^12.5.0
  firebase_core: ^4.14.0
  flutter:
    sdk: flutter
  flutter_local_notifications: 20.1.0
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: 3.0.0
  # google_mobile_ads is used only for its bundled Google UMP consent
  # APIs (ConsentInformation/ConsentForm) — the locked ad network is
  # AppLovin MAX (applovin_max above), not AdMob. Pinned to 7.0.0: newer
  # majors need Dart >=3.10, past this machine's 3.9.2.
  google_mobile_ads: 7.0.0
  intl: ^0.20.2
  isar_community: 3.3.0
  isar_community_flutter_libs: 3.3.0
  meta: ^1.15.0
  path_provider: ^2.1.5
  riverpod_annotation: 3.0.0
  # Share card (Phase 5). Pinned below 13.x: that needs Dart >=3.10, past
  # this machine's 3.9.2 — same story as every other Phase 0/3/4 pin.
  share_plus: 12.0.2
  timezone: ^0.10.0

dev_dependencies:
  build_runner: 2.7.1
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7
  flutter_test:
    sdk: flutter
  image: ^4.9.2
  isar_community_generator: 3.3.0
  riverpod_generator: 3.0.0
  very_good_analysis: ^7.0.0

# flutter_launcher_icons config — run `dart run flutter_launcher_icons`
# (or `make icons`) after replacing assets/icon/app_icon.png.
flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#3D5AFE"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  min_sdk_android: 24

# flutter_native_splash config — run `dart run flutter_native_splash:create`
# (or `make splash`) after replacing assets/icon/app_icon.png.
flutter_native_splash:
  color: "#F4F5FB"
  color_dark: "#12131C"
  image: "assets/icon/splash_logo.png"
  android_12:
    color: "#F4F5FB"
    color_dark: "#12131C"
    image: "assets/icon/splash_logo.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
