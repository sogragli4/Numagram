/// One numbered section within a `WordPuzzleTrack` (e.g. "Tarih 3") — a
/// fixed set of puzzles that unlocks once the previous section in that
/// same track is fully solved.
///
/// Placeholder — Faz 0 skeleton only. See "Kelime Bulmacası" in CLAUDE.MD
/// (bölüm 4 of the proposal artifact).
class WordPuzzleSection {
  const WordPuzzleSection();
}
