/// Checks a single typed letter against a `WordClue`'s answer and returns
/// the updated cell state — the word-puzzle equivalent of `validate_move`'s
/// `applyMove`, but per-letter and with no heart cost (see CLAUDE.MD
/// bölüm 2 of the proposal artifact for why).
///
/// Placeholder — Faz 0 skeleton only.
void validateLetter() {}
