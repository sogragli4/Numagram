/// Spends one of the player's daily free interest/category changes (2 per
/// day), or requires a rewarded-ad watch beyond that — the same date-keyed
/// daily-counter pattern as `AppSettings.archiveUnlocksCountFor`.
///
/// Placeholder — Faz 0 skeleton only.
class SpendCategoryChange {
  const SpendCategoryChange();
}
