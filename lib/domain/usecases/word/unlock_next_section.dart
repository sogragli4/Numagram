/// Marks the next `WordPuzzleSection` in a track as unlocked once every
/// puzzle in the current one is solved — the word-puzzle equivalent of
/// `AppColorTheme.isUnlockedAt` / `FreePlaySizePreset.isUnlockedAt`, but
/// keyed to completion rather than a streak.
///
/// Placeholder — Faz 0 skeleton only.
class UnlockNextSection {
  const UnlockNextSection();
}
