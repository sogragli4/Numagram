/// The multi-select ilgi alanı anketi shown once, before the player's
/// first word-puzzle track, right after the shared tutorial — same
/// `hasSeenX`-flag pattern as `TutorialScreen`'s `hasSeenTutorial`.
///
/// Placeholder — Faz 0 skeleton only. See "Kelime Bulmacası" in CLAUDE.MD
/// (bölüm 3 of the proposal artifact).
class InterestSurveyScreen {
  const InterestSurveyScreen();
}
