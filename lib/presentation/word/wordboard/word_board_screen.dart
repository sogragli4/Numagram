/// The word puzzle's playable screen — numbered clue list, per-letter
/// input boxes, instant correct/incorrect feedback. The word-puzzle
/// equivalent of `presentation/game/board_screen.dart`, but a `ListView`
/// rather than a `CustomPainter` grid (far fewer, non-performance-critical
/// cells).
///
/// Placeholder — Faz 0 skeleton only. See "Kelime Bulmacası" in CLAUDE.MD
/// (bölüm 2 of the proposal artifact).
class WordBoardScreen {
  const WordBoardScreen();
}
