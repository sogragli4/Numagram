/// The app's new entry point once both game modes exist — shows two
/// cards ("Nonogram" and "Kelime Bulmacası"), each opening that game's
/// own flow. Will replace `DailyScreen` as `_HomeRouter`'s post-tutorial
/// destination in `main.dart`.
///
/// Placeholder — Faz 0 skeleton only. See "Kelime Bulmacası" in CLAUDE.MD
/// (bölüm 9 of the proposal artifact).
class GamePickerScreen {
  const GamePickerScreen();
}
